# [LAB-03] 머신러닝 예측모델 캡스톤

## 뉴욕 맨해튼 임대료 예측 모델 구축

> 아이티윌 데이터분석 부트캠프 56기    
> 이광호 강사 (leekh4232@gmail.com)

본 과제는 StreetEasy의 뉴욕 맨해튼 임대 매물 데이터를 활용하여
데이터 품질 점검부터 탐색적 분석, 전처리, 모델링, 튜닝, 진단, 해석까지
머신러닝 예측 모델 구축의 전 과정을 처음부터 끝까지 수행하는 평가 과제입니다.

각 미션은 앞 미션의 결과물을 입력으로 받습니다.
따라서 미션을 건너뛰면 그다음 미션이 성립하지 않습니다.
13개 미션을 순서대로 통과하고 나면, 답이 아니라 하나의 완성된 분석 프로젝트가 손에 남습니다.

> **"이 매물의 월세는 왜 이 가격인가, 그리고 우리는 그 답을 얼마나 믿을 수 있는가?"**

이 과제의 목적은 "성능 좋은 모델을 찾는 것"이 아니라
**"모델이 내놓은 숫자에 책임질 수 있는 근거를 남기는 것"** 입니다.

- 본 과제는 팀/개인 단위 모두 수행 가능합니다.
- 미션마다 제시된 산출물(표·그래프·저장 파일)을 반드시 남겨야 다음 미션의 근거가 됩니다.

### 데이터 불러오기

```py3
from hossam import load_data
origin = load_data("streeteasy")
```

데이터 설명은 load_data() 함수가 출력하는 설명을 참고하세요.


## 1부. 데이터를 이해하는 단계

### 미션 1. **"이 표본은 무엇을 담고 있나?"**

한 줄도 고치기 전에, 받은 자료가 무엇인지부터 확인하자.

먼저 변수의 성격을 확정해야 한다.
`no_fee`부터 `has_gym`까지 여덟 개 변수는 0과 1 두 값뿐이다.
숫자로 저장되어 있지만 이 값들의 평균은 "비율"일 뿐이라 중심이나 꼬리를 논할 대상이 아니다.
연속형 통계에 섞여 들어가지 않도록 시작할 때 범주형으로 바꿔 두자.

그다음 아래 항목을 차례로 점검한다.

- 행과 열은 몇 개이며, 각 변수의 자료형은 무엇인가
- 완전히 같은 행이 중복 기록되어 있지는 않은가
- 비어 있는 칸은 없는가
- 숫자형 변수의 최솟값·최댓값이 현실에서 가능한 범위 안에 있는가
- 침실 수·욕실 수·층·건물 연수가 0으로 기록된 매물은 각각 몇 건이며, 그 **0은 무엇을 뜻하는가**
- 명목형 변수는 몇 개의 수준으로 이루어져 있고, 특정 수준에 지나치게 몰려 있지 않은가

특히 명목형 변수 하나는 모든 행이 같은 값을 갖는다.
값이 하나뿐이면 그 변수는 어떤 행과 어떤 행도 구분해 주지 못한다.
그 변수를 찾아낸 뒤 왜 설명변수가 될 수 없는지 한 문장으로 정리한다.

> **출제 의도**
> 분석 전에 "이 값이 말이 되나"를 먼저 묻는 습관을 들입니다. 여기서 발견한 것을 고치지 않고 기록해 두는 것까지가 이 단계의 일입니다. 3단원 전처리의 근거는 전부 여기서 나옵니다.

**남겨야 할 산출물** — 최솟값·최댓값 표, 중복·결측 점검 결과, 명목형 수준별 빈도표, 발견 사항 목록

### 미션 2. **"어떤 변수가 회귀를 방해할까?"**

숫자형 변수 전체의 기술통계량을 낸 뒤 다음 네 가지를 함께 읽는다.

- **평균과 중앙값의 거리** — 평균이 중심 역할을 못 하는 변수는 무엇인가
- **왜도(skew)** — 꼬리가 어느 쪽으로, 얼마나 길게 늘어져 있는가
- **첨도(kurt)** — 중심에 몰려 있으면서 극단값도 함께 있는 변수는 무엇인가
- **이상치 비율** — 사분위 범위를 벗어난 값이 몇 %인가

네 지표가 같은 변수를 가리키는지 확인한다.
하나만 튀는 것과 넷이 함께 튀는 것은 의미가 다르다.

종속변수 `rent`의 분포도 반드시 확인하자.
월세가 고르게 퍼져 있는지, 한쪽에 몰려 있는지 그림으로 보고
"이 상태로 회귀를 돌려도 되는가"를 스스로에게 묻는다.

마지막으로, 로그 변환을 검토해야 할 변수를 우선순위대로 정리하자.
이때 채점 대상은 "왜 써야 했는가"에 대한 설명이다. "로그를 썼다"는 기록만으로는 부족하다.
변환은 여기서 하지 않는다. 대상만 확정하고 3단원으로 넘기자.

> **출제 의도**
> 지표를 겹쳐 읽는 연습입니다. 로그 변환은 기법이 아니라 판단의 결과여야 합니다.

**남겨야 할 산출물** — 기술통계량 표(평균·중앙값·왜도·첨도·이상치 비율·변환 판정), 종속변수 분포 그래프, 로그 변환 후보 목록과 근거


## 2부. 변수를 고르는 단계

### 미션 3. **"월세를 가르는 변수는 무엇인가?"**

이제 변수 하나하나를 종속변수와 마주 세워 보자.
변수의 유형에 따라 쓰는 방법이 다르다.

#### 1) 연속형 독립변수 → 월세

상관분석을 수행해 계수의 크기·부호·유의성을 함께 정리한다.
이때 Pearson과 Spearman 중 무엇을 썼는지, 왜 그것을 골랐는지 반드시 밝힌다.
(힌트: 미션 2에서 확인한 분포가 근거가 된다.)

#### 2) 2집단 명목형 독립변수 → 월세

편의시설 여덟 종은 모두 두 값뿐이므로 두 집단 평균 비교의 대상이다.
정규성 점검 결과에 따라 어떤 검정이 채택되었는지 확인한 다음
**유의확률과 효과크기를 함께** 읽는다.

여기서 반드시 걸릴 변수가 하나 있다.
월세에 아무런 차이를 만들지 못하는 편의시설이 무엇인지 찾아낸 뒤
그 변수를 어떻게 처리할지 결론을 내린다.

#### 3) 3집단 이상 명목형 독립변수 → 월세

`neighborhood`에 분산분석을 적용한다.
전체적으로 차이가 있다는 결과가 나올 것이다.

그런데 이 변수는 수준이 32개다.
사후검정으로 "어느 동네와 어느 동네가 다른지"를 가리려면
32개를 둘씩 짝지은 496개 조합을 전부 봐야 한다.
실제로 이 표를 만들어 보고 이대로 결과로 내놓을 수 있는지 판단하자.

> 이 지점에서 내린 "그대로는 못 쓴다"는 결론이 **미션 6의 출발점**이 된다.

> **출제 의도**
> 변수 유형별로 맞는 도구를 고르고 결과를 유의성 판정에서 멈추지 않고 효과의 크기까지 읽는 연습입니다. 그리고 통계적으로는 유의한데 실무적으로는 쓸 수 없는 변수가 있다는 것을 직접 겪게 합니다.

**남겨야 할 산출물** — 상관분석 결과표(강도 순 정렬), 편의시설별 검정 결과표, 동네 분산분석 결과, 제외 후보 변수와 그 근거


### 미션 4. **"겹치는 축은 무엇인가?"**

독립변수끼리도 서로를 얼마나 설명하는지 확인한다.
연속형 독립변수 전체의 상관행렬을 만들어 강한 상관으로 묶이는 **덩어리**를 찾는다.

이 데이터에는 사실상 같은 이야기를 하는 변수 세 개가 한 덩어리로 묶여 있다.
그 세 개가 무엇인지 찾아내고 왜 이들이 서로를 대신할 수 있는지 설명하자.

그리고 다음 질문에 답한다.

- 셋을 모두 모델에 넣으면 무슨 일이 생기는가
- 셋 중 하나만 남긴다면 무엇을 남길 것인가, 그 기준은 무엇인가
- 나머지 둘이 갖고 있던 정보를 버리지 않고 살릴 방법은 없는가

마지막으로, 지금까지의 결과를 모아 **"모델에 넣을 1순위 변수"** 를 하나 지목하고
그 근거를 세 줄로 정리하자.

> **출제 의도**
> 상관이 높은 변수를 덩어리로 인식하고 대표를 세우는 사고를 훈련합니다. 무조건 지우고 마는 것과는 다릅니다. "버린 정보를 파생변수로 되살린다"는 다음 단계의 발상이 여기서 나와야 합니다.

**남겨야 할 산출물** — 독립변수 간 상관행렬(히트맵), 중복 덩어리 요약표, 대표 변수 선정 근거, 1순위 변수 보고 문장


## 3부. 데이터를 다시 만드는 단계

### 미션 5. **"없는 변수를 만든다"**

원본 열여섯 개만으로는 부족하다.
미션 4에서 "버리기 아깝다"고 판단한 정보를 비율과 요약으로 되살리자.

아래 관점을 참고해 **연속형 파생변수를 4종 이상** 만든다.
(그대로 써도 되고, 더 나은 것을 직접 설계해도 된다.)

| 관점              | 아이디어                              |
| --------------- | --------------------------------- |
| 방 하나를 얼마나 넓게 쓰나 | 전용 면적 ÷ (침실 수 + 욕실 수)             |
| 편의시설을 몇 개나 갖췄나  | `has_*` 일곱 개의 합 (0~7)             |
| 동네에서 큰 집인가 작은 집인가 | 전용 면적 ÷ **그 동네** 평균 전용 면적         |
| 동네에서 높은 층인가     | 층 ÷ **그 동네** 평균 층                 |

만든 뒤에는 반드시 점검한다.

- 분모로 쓴 변수의 최솟값이 0은 아닌가
- 결과에 결측이나 **무한대(inf)** 가 생기지 않았는가

무한대가 생겼다면 그대로 두고 넘어갈 수 없다.
전체의 몇 %인지 세어 본 뒤 다음 원칙에 따라 처리하자.

- 5% 미만 → 해당 행을 제거한다 (정보 손실이 작다)
- 5% 이상 → 제거하면 손실이 크므로 결측으로 바꾸고 대체 여부를 따로 판단한다

정리가 끝난 뒤, 깨끗해진 1차 파생변수를 재료로 2차 파생변수를 하나 더 만들자.
(예: 방 하나당 면적 ÷ 그 동네 평균 "방 하나당 면적")
순서를 지키는 이유는 분명하다. 오염된 값으로 만든 파생은 오염을 물려받는다.

> **출제 의도**
> 파생변수는 만들고 → 점검하고 → 정리하고 → 다시 만드는 순환입니다. 한 번 만들면 끝나는 작업이 아닙니다. 나눗셈 하나가 만들어 내는 무한대를 직접 겪고 처리 기준을 말로 남겨 보게 합니다.

**남겨야 할 산출물** — 파생변수 정의표(이름·계산식·의미), 분모 최솟값 점검 결과, 무한대 발생 건수와 비율, 처리 기준과 처리 후 데이터 크기


### 미션 6. **"동네 32개를 다시 그린다"**

미션 3에서 우리는 `neighborhood`를 그대로 쓸 수 없다고 결론 내렸다.
그렇다고 버리면 위치 정보가 통째로 사라진다. 맨해튼에서 위치를 버리는 분석은 성립하지 않는다.

버리지 말고 다시 그리자. 두 단계로 진행한다.

**① 희소 수준 병합**
동네별 매물 수를 세어 보면 579건짜리 동네와 1건짜리 동네가 함께 있다.
매물이 몇 건뿐인 동네는 그 동네를 대표하지 못한다.
전체의 1% 미만인 동네를 하나의 `Other`로 묶은 뒤 수준이 몇 개로 줄었는지 확인한다.

**② 동네를 성격별로 묶기 (군집)**
그래도 수준이 스무 개 넘게 남는다. 한 번 더 줄여야 한다.

이때 "동네별 평균 월세로 묶으면 되지 않나" 하는 생각이 들 것이다.
**하면 안 된다.** 종속변수로 만든 변수를 독립변수로 넣는 것은
정답을 미리 알려 주고 시험을 보는 것과 같다. (데이터 누수, data leakage)

대신 월세를 뺀 나머지 특성으로 동네의 프로파일을 만든다.
동네별 평균 전용 면적·평균 층·평균 건물 연수·평균 역까지 시간·편의시설 보유율 등이다.
이 프로파일에 군집분석을 적용해 동네를 몇 개의 권역으로 묶자.

- 최적 군집수를 지표로 확인하되 지표가 제시한 수를 그대로 쓸지는 목적에 비추어 판단한다
- 군집을 확정한 뒤 권역별 매물 수·평균 월세를 표로 내어 묶임이 말이 되는지 확인한다
- 각 권역에 어떤 동네들이 들어갔는지 나열하고 권역마다 이름을 붙여 보자

여기에 더해 **범주형 파생변수를 1종 이상** 만든다.
(예: 역세권 여부 — 도보 N분 이내, 신축 여부 — 준공 N년 이내)
기준선을 몇으로 잡았는지와 그 근거를 반드시 밝힌다.

> **출제 의도**
> 고차원 명목형 변수를 다루는 정공법을 익힙니다. 그리고 가장 성능이 잘 나오는 방법이 가장 정직한 방법은 아닙니다. 데이터 누수를 스스로 피해 가는 경험이 이 미션의 핵심입니다.

**남겨야 할 산출물** — 동네별 매물 수 분포, 병합 후 수준 수, 군집수 결정 근거, 권역별 구성 동네 목록과 매물 수·평균 월세 표, 권역 명명, 범주형 파생변수 정의와 기준


### 미션 7. **"모델에 넣을 변수를 확정한다"**

만든 파생변수를 **검증 없이 모델에 넣지 않는다.**
미션 3에서 원본 변수에 했던 것과 똑같은 절차를 파생변수에도 적용한다.

- 연속형 파생 → 종속변수 : 상관분석 (강도·부호·유의성)
- 2집단 범주형 파생 → 종속변수 : 두 집단 평균 비교
- 3집단 이상 범주형 파생 → 종속변수 : 분산분석 + 사후검정
- 파생변수끼리 : 상관행렬로 중복 점검

검증 결과 중에는 의도와 반대로 나오는 변수가 있을 것이다.
"넓을수록 비쌀 것"이라 생각하고 만든 변수의 부호가 음수로 나오는 식이다.
지우기 전에 왜 그런 방향이 나왔는지 먼저 설명해 보자. 설명이 되면 그 변수는 살아남을 자격이 있다.

이제 최종 투입 변수를 확정한다. 변수마다 **채택 또는 제외의 사유를 한 줄씩** 붙인다.

- 원본 유지 : 무엇을 근거로 남기는가
- 파생 채택 : 어떤 검증을 통과했는가
- 제외 : 비유의인가, 효과크기가 없는가, 중복인가, 누수 위험인가

확정한 뒤 마지막 처리를 한다.

1. **로그 변환** — 미션 2와 이번 미션의 판정에 따라 대상 변수를 변환한다. **종속변수도 대상이다.**
2. **라벨링** — 범주형을 모델이 읽을 수 있는 정수로 바꾼다.
3. **저장** — 변환 전 상태와 변환 후 상태를 각각 파일로 남긴다.

> 라벨링을 거치면 범주형이 정수가 된다. 이 상태로 모델에 넣으면 크기를 가진 숫자로 학습된다.
> 다음 미션에서 데이터를 불러오면 **반드시 범주형으로 되돌려 놓고** 시작해야 한다.

> **출제 의도**
> 만든 변수를 스스로 검증하는 태도를 봅니다. 여기서 확정한 변수 목록과 저장 파일이 8번 미션부터 14번 미션까지의 유일한 입력이므로 이 미션이 프로젝트 전체의 허리입니다.

**남겨야 할 산출물** — 파생변수 검증 결과표 전체, 변수별 채택/제외 사유표, 로그 변환 대상과 판정 근거, 저장 파일 2종(변환 전 / 로그·라벨링 후)


## 4부. 모델을 만드는 단계

### 미션 8. **"기준선을 세운다"**

전처리를 마친 데이터를 불러와 **훈련용 8 : 검증용 2**로 나눈다.
이후 모든 미션에서 같은 분할을 써야 성능을 비교할 수 있으므로 난수 시드를 고정하자.

이어서 대표 모델 11종의 베이스라인을 구축해 성능 평가 결과를 한 표로 모은다.
(모델별로 필요한 전처리 조합이 다르다는 점에 유의한다.)

결과표를 놓고 다음을 판단해 보자.

- 선형 계열 모델보다 못한 모델은 몇 개이며 무엇인가
- 가지치기 없이 자란 트리 한 그루의 성능은 어떠한가
- 같은 트리를 여러 그루 키워 평균 낸 모델은 얼마나 나아졌는가
- 1위와 근소한 차이로 묶이는 **상위 그룹**은 몇 개인가

마지막으로 튜닝 후보로 가져갈 계열을 정하고 근거를 쓴다.
"1위라서"는 근거가 아니다. 1위와의 격차가 얼마이고 왜 그 격차가 의미 있는지를 써야 한다.

> **출제 의도**
> 기준선(baseline)의 의미를 체감하게 합니다. 이보다 못하면 쓸 이유가 없는 선을 먼저 긋는 일입니다. 그리고 모델 선택을 한 장의 비교표 위에서 하는 훈련입니다. 트리 한 그루와 숲의 차이를 숫자로 확인합니다.

**남겨야 할 산출물** — 11종 성능 비교표(순위·주지표·보조지표), 계열별 성능 해석, 튜닝 후보 선정 근거


### 미션 9. **"튜닝이 얼마를 돌려주는지 잰다"**

기본값으로 학습한 모델은 아직 그 모델의 실력이 아니다.
하이퍼파라미터를 탐색해 각 모델이 낼 수 있는 최선을 확인하자.

- 훈련 데이터를 **5겹으로 나눈 교차검증** 점수로 조합을 고른다
- 검증 데이터는 탐색에 쓰지 않는다. 왜 그런지 설명하자
- 열한 종을 모두 튜닝해 모델별 최적 조합과 교차검증 점수를 기록한다

튜닝이 끝나면 미션 8의 표와 나란히 놓고 비교한다.

- 가장 크게 좋아진 모델은 무엇이며, 왜 그렇게 좋아졌는가
- 미션 8에서 무너졌던 모델은 회복되었는가. 회복되었다면 무엇이 문제였는가
- 상위 그룹의 구성은 어떻게 바뀌었는가
- 1위 모델의 성능은 튜닝 전후로 얼마나 올랐는가

마지막 질문에 답하자.
**"튜닝을 더 밀어붙일 것인가, 여기서 접을 것인가?"**
들인 시간 대비 돌려받은 성능을 근거로 판단하고 그 판단을 문장으로 남긴다.

> **출제 의도**
> 튜닝의 효과를 막연한 기대가 아니라 측정값으로 다루게 합니다. 무너졌던 성능의 원인이 사실은 설정에 있었다는 것을 여기서 확인하게 됩니다.

**남겨야 할 산출물** — 튜닝 후 11종 순위표, 모델별 최적 조합, 튜닝 전후 성능 비교표, 튜닝 지속 여부 판단과 근거


## 5부. 모델을 검증하고 해석하는 단계

### 미션 10. **"점수가 배운 것인지 외운 것인지 가른다"**

순위표는 점수만 보여 준다. 그 점수가 일반화된 실력인지 암기인지는 알려 주지 않는다.

상위 모델들을 대상으로 과적합을 진단하자.
훈련 점수와 교차검증 점수의 **격차**를 잰다.

- 진단이 과대적합으로 판정하는 기준 비율은 얼마인가
- 여러 지표 중 일부 지표에서만 과적합 판정을 받은 모델이 있는가
- 훈련 데이터에서 가장 높은 점수를 낸 모델이 검증에서도 가장 높은가

진단 결과를 표로 정리한 뒤 배포 후보에서 탈락시킬 모델을 지목하자.
탈락 사유는 "점수가 낮아서"가 아니라 "훈련과 검증의 격차가 커서"여야 한다.

그리고 살아남은 모델 중에서 하나를 고른다.
성능만이 기준이 아니다. 안정성·학습 시간·해석 가능성을 함께 저울에 올려
무엇을 얼마나 중요하게 봤는지 쓴다.

> **출제 의도**
> "검증 점수가 제일 높은 모델을 쓴다"는 습관을 깨는 미션입니다. 훈련 점수와 교차검증 점수의 격차가 왜 성능 자체보다 중요한 신호인지 체감하게 합니다.

**남겨야 할 산출물** — 모델별 과적합 진단표(훈련/교차검증/검증 점수와 격차), 탈락 모델과 사유, 최종 모델 선정 근거


### 미션 11. **"변수를 줄이고 다시 학습한다"**

모델을 정했으니 이제 투입한 변수가 전부 필요한지 확인한다.

선택한 모델의 변수 중요도를 계산해 큰 순서대로 정렬한다.

- 모델이 쓰는 정보의 가장 큰 몫을 혼자 만들어 내는 변수는 무엇인가
- 중요도를 누적했을 때 **95%를 넘어서는 지점**은 어디인가
- 그 기준으로 잘라 내면 몇 개의 변수가 빠지는가

빠진 변수를 확인하고 왜 빠졌는지 설명한다.
미션 5에서 공들여 만든 파생변수가 여기서 빠질 수도 있다.
그렇다면 그 변수를 만든 것은 헛수고였는지, 아니면 다른 의미가 있었는지 답해야 한다.

남은 변수만으로 모델을 다시 학습하고 다시 튜닝한 뒤
줄이기 전과 성능을 비교한다.

- 성능은 얼마나 떨어졌는가 (또는 올랐는가)
- 과적합 진단 결과는 어떻게 바뀌었는가
- 변수를 줄인 대가로 무엇을 얻었는가 (학습 속도, 수집 비용, 설명의 단순함)

최종 배포 후보를 **변수 개수까지 포함해** 한 문장으로 확정하자.

> **출제 의도**
> 변수는 늘릴 때보다 줄일 때 실력이 드러난다는 것을 경험하게 합니다. 성능 손실과 운영 비용을 함께 저울에 올리는 판단을 훈련합니다.

**남겨야 할 산출물** — 변수 중요도표(누적 비율 포함), 탈락 변수와 해석, 축소 전후 성능·진단 비교표, 최종 모델 확정 문장


### 미션 12. **"변수가 월세를 어느 쪽으로 미는지 본다"**

변수 중요도는 "얼마나 중요한가"만 알려 준다.
"어느 쪽으로 미는가"는 알려 주지 않는다.

최종 모델에 SHAP 분석을 적용해 매물 한 건 한 건에 대해
각 변수가 예측을 얼마나·어느 방향으로 밀었는지 계산하자.

결과를 요약해 다음을 정리한다.

- 영향력 상위 두 변수가 전체의 몇 %를 차지하는가
- 월세를 평균적으로 밀어 올리는 변수와 끌어내리는 변수는 각각 무엇인가
- 같은 변수라도 매물마다 기여가 다르다. 가장 심하게 출렁이는 변수는 무엇인가
- 영향력은 큰데 **평균 기여가 거의 0인 변수**가 있는가

마지막 항목은 조금 더 들여다봐야 한다.
평균 기여가 0이라는 것은 효과가 없다는 뜻이 아니라
어떤 매물에서는 올리고 어떤 매물에서는 내려서 서로 상쇄됐다는 뜻이다.
그런 변수에는 "값이 커지면 월세가 오른다"는 한 줄을 붙일 수 없다.
해당하는 변수를 찾아내 왜 한 줄로 못 박을 수 없는지 설명한다.

> **출제 의도**
> 중요도와 방향은 다른 질문이라는 것, 그리고 평균이 0인 것과 효과가 없는 것은 다르다는 것을 구분하게 합니다. 여기서 걸러 낸 변수가 다음 미션에서 그림으로 열립니다.

**남겨야 할 산출물** — SHAP 영향력 요약표(평균 절대기여·평균 기여·변동성), 방향별 변수 정리, 한 줄로 정리할 수 없는 변수와 사유


### 미션 13. **"그림으로 열어 보고 결론을 낸다"**

표가 0으로 뭉갠 자리를 그림으로 열어 보자.
아래 네 가지 시각화를 만들고 각각에서 무엇을 새로 알게 되었는지 쓴다.

| 그림                     | 읽어야 할 것                                |
| ---------------------- | -------------------------------------- |
| **Bar Plot**           | 변수별 영향력 크기와 채택·탈락 경계                   |
| **Beeswarm Plot**      | 변수값의 높낮이(색)와 기여 방향의 관계                 |
| **Dependence Plot**    | 변수값이 커질 때 기여가 어떻게 변하는가 — 방향이 뒤집히는 구간은? |
| **Waterfall Plot**     | 개별 매물 한 건의 예측이 어떻게 조립되는가               |

특히 다음을 확인한다.

- Beeswarm에서 점이 한 가지 색으로만 찍혀 방향을 읽을 수 없는 변수는 무엇인가. 왜 그런가
- 미션 12에서 "한 줄로 못 박을 수 없다"던 변수를 Dependence Plot으로 열면 무엇이 보이는가
- 미션 6에서 만든 권역 변수를 권역별로 갈라 보면, 월세를 가장 세게 끌어내리는 권역은 어디인가
- Waterfall Plot을 **가장 싼 매물 / 중간 매물 / 가장 비싼 매물** 세 건에 대해 그려 비교하면 무엇이 다른가


### 마무리 — 다섯 줄 결론

지금까지 남긴 산출물을 근거로 아래 다섯 가지에 각각 한두 문장으로 답해보자.

1. **최종 모델** — 무엇을 몇 개의 변수로 골랐는가
2. **성능** — 검증 성능은 얼마이며, 기본값으로 학습했을 때보다 얼마나 나아졌는가
3. **월세를 가르는 구조** — 월세를 가장 세게 밀어 올리는 변수와 끌어내리는 변수는 무엇인가
4. **위험했던 지점** — 데이터 누수나 과적합이 될 뻔했던 대목은 어디였고, 어떻게 피했는가
5. **믿을 수 있는 범위** — 이 모델을 어디까지 쓸 수 있는가

5번은 반드시 쓴다.
표본이 맨해튼 3,500여 건이고 특정 시점의 스냅숏이라는 사실이
**이 모델의 사용 범위를 어디까지로 제한하는지** 밝혀야 한다.

> **출제 의도**
> 그림은 표가 뭉갠 것을 열어 보는 도구입니다. 그리고 마지막 결론은 13개 미션이 하나의 이야기로 이어지는지를 봅니다. 분량은 짧아도 됩니다. 채점 대상은 문서의 완성도가 아니라 앞 미션의 산출물을 근거로 답했는가입니다. 성능 숫자보다 "어디까지 믿을 수 있는가"를 쓸 줄 아는 것이 이 과제의 최종 목표입니다.

**남겨야 할 산출물** — 시각화 4종과 각각의 해석, 권역별 기여 비교, 대표 매물 3건의 예측 분해, 다섯 줄 결론


### 채점 기준 (총 100점)

각 미션은 부분 수행 시에도 분석 흔적이 확인되면 최소 점수를 부여하며 미션을 수행하지 않은 경우에만 0점 처리한다.

| 미션                | 배점 | 평가 포인트                                     | 감점 기준                                                      |
| ----------------- | -: | ------------------------------------------ | ---------------------------------------------------------- |
| 1. 표본 구조·품질 점검    |  5 | 타입 확정, 중복·결측·범위 점검, 0의 의미 해석, 상수 변수 발견     | 이진형 타입 변환 누락(-2), 0의 의미 미해석(-2), 상수 변수 미발견(-2)             |
| 2. 기술통계·분포 판정     |  5 | 네 지표 겹쳐 읽기, 종속변수 분포, 로그 대상 선정 근거           | 지표 나열에 그침(-2), 종속변수 분포 미확인(-2), 변환 이유 미설명(-2)              |
| 3. 이변량 분석         |  7 | 변수 유형별 적합한 검정, 계수 선택 근거, 효과크기 해석           | 계수 선택 이유 없음(-2), 효과크기 미해석(-2), 비유의 변수 미발견(-3)              |
| 4. 다변량 중복 점검      |  6 | 중복 덩어리 식별, 대표 선정 기준, 정보 보존 발상              | 상관행렬만 제시(-2), 대표 선정 근거 없음(-2), 후속 처리 방향 미제시(-2)            |
| 5. 연속형 파생변수 설계    |  9 | 4종 이상 설계, 분모·무한대 점검, 처리 기준, 2차 파생          | 파생 3종 이하(-3), 무한대 점검 누락(-3), 처리 기준 미기재(-2), 2차 파생 없음(-2)   |
| 6. 고차원 명목형 재설계    | 10 | 희소 병합, 누수 없는 군집, 군집수 근거, 권역 해석             | 누수 발생(-5), 병합 없이 군집(-3), 군집수 근거 없음(-2), 권역 해석 부재(-3)       |
| 7. 투입 변수 확정       | 10 | 파생 전수 검증, 채택·제외 사유, 변환·라벨링·저장              | 파생 검증 누락(-4), 사유표 없음(-3), 종속변수 변환 누락(-2), 저장 파일 없음(-2)     |
| 8. 베이스라인 구축·모델 비교 |  9 | 분할 고정, 11종 학습, 계열별 성능 해석, 튜닝 후보 선정 근거      | 시드 미고정(-2), 일부 계열 누락(-3), 표만 제시(-2), 후보 근거 부실(-2)          |
| 9. 하이퍼파라미터 튜닝     |  8 | 교차검증 기반 탐색, 전후 비교, 지속 여부 판단                | 검증 데이터로 탐색(-4), 전후 비교 없음(-3), 판단·근거 부재(-3)                 |
| 10. 과적합 진단        |  9 | 격차 기반 판정, 탈락 사유, 다기준 모델 선정                 | 성능만으로 선정(-4), 격차 미해석(-3), 탈락 사유 부재(-3)                     |
| 11. 변수 중요도·축소     |  8 | 누적 기준 적용, 탈락 변수 해석, 축소 전후 비교, 비용 관점        | 축소 후 재학습 없음(-3), 탈락 변수 미해석(-2), 전후 비교 없음(-2), 비용 관점 부재(-2) |
| 12. SHAP 영향력 해석   |  7 | 방향·변동성 구분, 평균 0의 의미 해석                     | 중요도와 동일 해석(-3), 방향 미구분(-2), 평균 0 변수 미발견(-3)                |
| 13. 시각화·다섯 줄 결론   |  7 | 4종 시각화 해석, 권역·개별 매물 분해, 결론 5항 작성, 사용 범위 명시 | 시각화 일부 누락(-3), 그림만 제시(-2), 결론 항목 누락(-2), 사용 범위 미기재(-2)     |

#### 가산점 (최대 5점)

| 항목                                       | 배점 |
| ---------------------------------------- | -: |
| 제시된 것 외의 파생변수를 직접 설계하고, 검증을 통해 채택 근거를 남긴 경우 | +2 |
| 예측값을 원 단위(USD)로 되돌려 잔차를 금액으로 해석한 경우      | +2 |
| 데이터 누수 위험을 스스로 발견하고 회피 과정을 기록한 경우        | +1 |

#### 감점요소

| 항목                                          |   감점 |
| ------------------------------------------- | ---: |
| 종속변수(월세)로 만든 값을 독립변수로 사용한 경우 (데이터 누수)       | -15 |
| 검증 데이터를 전처리 기준 산출이나 하이퍼파라미터 탐색에 사용한 경우      | -10 |
| 라벨링된 범주형을 되돌리지 않아 크기를 가진 숫자로 학습시킨 경우        |  -5 |
| 미션 간 훈련/검증 분할 기준이 달라 성능 비교가 성립하지 않는 경우      |  -5 |
